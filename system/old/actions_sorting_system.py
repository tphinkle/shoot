



class ActionsSortingSystem:

    '''
    * Action processing pipeline:
        0. Create/Register
            - e.g., create action from controller input
            - e.g., create action from AI input
        1. Validate
            - Check if action is permissable; if not, filter out
        2. Sort
            - Sort actions so they are processed in correct order
        3. Process
            - Perform the action

    * Third step in the action processing pipeline.

    * Actions are sorted so they are processed in the correct order.
    '''




    def __init__(self):
        pass



    def SortActions(self, world):

        pass
